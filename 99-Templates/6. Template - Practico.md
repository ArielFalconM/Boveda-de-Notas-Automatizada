<%*
const currentFile = tp.config.target_file;
let nombreTema = "";
await new Promise(r => setTimeout(r, 400));

//Búsqueda del tema del practico.
const recentFiles = app.workspace.getLastOpenFiles();
if (recentFiles.length > 1) {
    const lastFile = app.vault.getAbstractFileByPath(recentFiles[1]);
    if (lastFile && lastFile.basename !== currentFile.basename) {
        nombreTema = lastFile.basename;
    }
}

// Si no se detecta el tema, se lo solicita al usuario.
if (!nombreTema || nombreTema === "Untitled") {
    nombreTema = await tp.system.prompt("¿A qué tema pertenece este práctico?");
}

//Input del usuario.
let nroPractico = await tp.system.prompt("Número o nombre de Práctico:");
let estados = ["⏳ Pendiente", "✏️ En proceso", "✔️ Completado"];
let estado_sel = await tp.system.suggester(estados, estados);

const FILE_PRACTICO = `${nombreTema} - Práctico ${nroPractico}`;
const FILE_MAPA_PRACTICO = `Borrador - Práctico ${nroPractico} - ${nombreTema}`;
const FILE_MAPA_TEORIA = `Teoría - ${nombreTema}`; // Asume que el mapa teórico existe

//Rename de la nota.
await tp.file.rename(FILE_PRACTICO);

//Creación de la pizarra de practico.
let t_excalidraw = app.metadataCache.getFirstLinkpathDest("Template-Excalidraw", "");
let c_mapas = app.vault.getAbstractFileByPath("40-Diagramas");
if (t_excalidraw && c_mapas) {
    let rutaDestinoMapa = `40-Diagramas/${FILE_MAPA_PRACTICO}.md`;
    let mapaExiste = app.vault.getAbstractFileByPath(rutaDestinoMapa);
    if (!mapaExiste) {
        try {
            await app.vault.copy(t_excalidraw, rutaDestinoMapa);
            new Notice(`✅ Pizarra Práctica [${FILE_MAPA_PRACTICO}] preparada.`);
        } catch (e) {
            new Notice("❌ Error al copiar la pizarra. Revisa la carpeta de diagramas.");
        }
    }
} else {
    new Notice("❌ ERROR: Faltan recursos base (Template-Excalidraw o carpeta 40-Diagramas).");
}

// Bypass: Forzar foco del cursor tras el renderizado de la UI
setTimeout(() => {
    const leaf = app.workspace.activeLeaf;
    if (leaf && leaf.view && leaf.view.editor) {
        const editor = leaf.view.editor;
        const lineas = editor.getValue().split('\n');
        
        // Busca dinámicamente cualquiera de tus encabezados principales
        const index = lineas.findIndex(l => 
            l.includes("Definición") || 
            l.includes("Descripción General") || 
            l.includes("Objetivos del Práctico") ||
            l.includes("Idea central") ||
            l.includes("Objetivo") || 
            l.includes("Breakdown")
        );
        
        if (index !== -1) {
            // Clava el cursor exactamente en la línea de abajo y fuerza el foco
            editor.setCursor({ line: index + 1, ch: 0 });
            editor.focus();
        }
    }
}, 400);
-%>
**📖 Tema Central:** [[<% nombreTema %>]]
**🧠 Consultar Teoría:** [[<% FILE_MAPA_TEORIA %>|Abrir Pizarra de Teoría]]
**✏️ Pizarra de Resolución:** [[<% FILE_MAPA_PRACTICO %>|Abrir Pizarra del Práctico]]

<div style="display: flex; flex-wrap: wrap; gap: 15px; padding: 12px 18px; margin-bottom: 20px; background-color: rgba(255, 255, 255, 0.05); border-radius: 8px; border-left: 4px solid #D095E8; font-size: 0.9em; align-items: center;">
  <span>📅 <b>Inicio:</b> <% tp.date.now("DD-MM-YYYY") %></span>
  <span style="color: rgba(255, 255, 255, 0.2);">|</span>
  <span>🏷️ <b>Tags:</b> #Practico</span>
  <span style="color: rgba(255, 255, 255, 0.2);">|</span>
  <span>🚥 <b>Estado:</b> <% estado_sel %></span>
</div>

## <span style="color: #D095E8;">🎯 Objetivos del Práctico</span>


---
## <span style="color: #D095E8;">📝 Resolución de Ejercicios</span>

### Ejercicio 1
**Letra:** 
**Resolución:** 


























Tags: #Practico
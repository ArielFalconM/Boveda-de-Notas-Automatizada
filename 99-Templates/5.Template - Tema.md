<%*
//Inputs del Usuario
let titulo = await tp.system.prompt("Tema a estudiar:");
let materias = ["Sistemas operativos", "Teoría de Lenguajes", "Bases de Datos", "Programación", "Física", "Matemáticas"];
let materia = await tp.system.suggester(materias, materias);
const FILE_TEMA = titulo;
const FILE_MAPA = `Teoría - ${titulo}`;
const FOLDER_MAPAS = "40-Diagramas";

// Rename de nota.
await tp.file.rename(FILE_TEMA);

// Creación de la pizarra.
let t_excalidraw = app.metadataCache.getFirstLinkpathDest("Template-Excalidraw", "");
if (t_excalidraw) {
    let rutaDestinoMapa = `${FOLDER_MAPAS}/${FILE_MAPA}.md`;
    let mapaExiste = app.vault.getAbstractFileByPath(rutaDestinoMapa);
    if (!mapaExiste) {
        try {
            await app.vault.copy(t_excalidraw, rutaDestinoMapa);
            new Notice(`✅ Pizarra [${FILE_MAPA}] preparada en ${FOLDER_MAPAS}`);
        } catch (error) {
            new Notice("❌ Error al crear la pizarra. Revisa la carpeta de diagramas.");
        }
    }
} else {
    new Notice("❌ ERROR: No se encontró 'Template-Excalidraw'.");
}

// Bypass: Forzar foco del cursor tras el renderizado de la UI
setTimeout(() => {
    const leaf = app.workspace.activeLeaf;
    if (leaf && leaf.view && leaf.view.editor) {
        const editor = leaf.view.editor;
        const lineas = editor.getValue().split('\n');
        
        // Busca dinámicamente cualquiera de tus encabezados principales
        const index = lineas.findIndex(l => 
            l.includes("Definición") || 
            l.includes("Descripción General") || 
            l.includes("Objetivos del Práctico") ||
            l.includes("Idea central") ||
            l.includes("Objetivo") || 
            l.includes("Breakdown")
        );
        
        if (index !== -1) {
            // Clava el cursor exactamente en la línea de abajo y fuerza el foco
            editor.setCursor({ line: index + 1, ch: 0 });
            editor.focus();
        }
    }
}, 400);
-%>
**🎓 Materia:** <% materia %>
**🗺️ Pizarra Teórica:** [[<% FILE_MAPA %>|Abrir Pizarra de Teoría]]

<div style="display: flex; flex-wrap: wrap; gap: 15px; padding: 12px 18px; margin-bottom: 20px; background-color: rgba(255, 255, 255, 0.05); border-radius: 8px; border-left: 4px solid #D095E8; font-size: 0.9em; align-items: center;">
  <span>📅 <b>Inicio:</b> <% tp.date.now("DD-MM-YYYY") %></span>
  <span style="color: rgba(255, 255, 255, 0.2);">|</span>
  <span>🏷️ <b>Tags:</b> #Tema</span>
</div>

## <span style="color: #D095E8;">🎯 Idea central</span>


---
## <span style="color: #D095E8;">🧠 Notas de Concepto</span>


---
## <span style="color: #D095E8;">📝 Ejercicios Relacionados</span>


---
## <span style="color: #D095E8;">📚 Fuentes</span>








































Tags: #Tema
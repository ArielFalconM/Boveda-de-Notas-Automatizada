<%*
const currentFile = tp.config.target_file;
let nombreProyecto = "";

await new Promise(r => setTimeout(r, 300));
const recentFiles = app.workspace.getLastOpenFiles();

// Buscar el proyecto en historial de tabs
if (recentFiles.length > 1) {
    const lastFile = app.vault.getAbstractFileByPath(recentFiles[1]);
    if (lastFile && lastFile.basename !== currentFile.basename) {
        nombreProyecto = lastFile.basename;
    }
}

// Si el historial falla, busca la nota del proyecto mediante backlinks
if (!nombreProyecto) {
    const backlinks = app.metadataCache.getBacklinksForFile(currentFile);
    if (backlinks && backlinks.data) {
        const paths = Object.keys(backlinks.data);
        if (paths.length > 0) {
            const sourceFile = app.vault.getAbstractFileByPath(paths[0]);
            if (sourceFile) nombreProyecto = sourceFile.basename;
        }
    }
}

// Si los dos mecanismos fallan, preguntar
if (!nombreProyecto || nombreProyecto === "Untitled") {
    nombreProyecto = await tp.system.prompt("Nombre del proyecto:", "Proyecto");
}


// Extracción del nombre del proyecto a partir del titulo de la nota.
let nombreBase = nombreProyecto.replace("Proyecto - ", "");

//Set de links
let linkMapa = `[[Mapa - ${nombreBase}|🗺️ Ver Mapa del Proyecto]]`;
let tituloFinal = `Bitácora - ${nombreBase} - ${tp.date.now("DD-MM-YYYY")}`;
let proyectoLink = `[[${nombreProyecto}]]`;

// Rename del titulo de la nota
await tp.file.rename(tituloFinal);

// Bypass: Forzar foco del cursor tras el renderizado de la UI
setTimeout(() => {
    const leaf = app.workspace.activeLeaf;
    if (leaf && leaf.view && leaf.view.editor) {
        const editor = leaf.view.editor;
        const lineas = editor.getValue().split('\n');
        
        // Busca dinámicamente cualquiera de tus encabezados principales
        const index = lineas.findIndex(l => 
            l.includes("Definición") || 
            l.includes("Descripción General") || 
            l.includes("Objetivos del Práctico") ||
            l.includes("Idea central") ||
            l.includes("Objetivo") || 
            l.includes("Breakdown")
        );
        
        if (index !== -1) {
            // Clava el cursor exactamente en la línea de abajo y fuerza el foco
            editor.setCursor({ line: index + 1, ch: 0 });
            editor.focus();
        }
    }
}, 400);
-%>
**📂 Proyecto:** <% proyectoLink %> | <% linkMapa %>

<div style="display: flex; flex-wrap: wrap; gap: 15px; padding: 12px 18px; margin-bottom: 20px; background-color: rgba(255, 255, 255, 0.05); border-radius: 8px; border-left: 4px solid #D095E8; font-size: 0.9em; align-items: center;">
  <span>🏷️ <b>Tags:</b> #Bitacora</span>
  <span style="color: rgba(255, 255, 255, 0.2);">|</span>
  <span>🕒 <b>Fecha y Hora:</b> <% tp.date.now("DD-MM-YYYY HH:mm") %></span>
</div>

## <span style="color: #D095E8;">🎯 Objetivo / Problema</span>


---
## <span style="color: #D095E8;">🛠️ Implementación o Resolución</span>


---
## <span style="color: #D095E8;">📓 Notas Técnicas</span>


---
## <span style="color: #D095E8;">🏁 Estado Final de la Sesión</span>

- [ ] Desarrollado.
- [ ] Testeado.
- [ ] Listo para commit/push.

**🐛 Bugs pendientes:**
























































Tags: #Bitacora
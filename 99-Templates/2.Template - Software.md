<%*
// 1-ENTRADA DE DATOS DEL USUARIO
let titulo = await tp.system.prompt("Nombre del software:");
if (!titulo) titulo = "Software_Sin_Nombre";

let categorias = ["IDE/Editor", "Motor Gráfico", "Terminal", "Gestor de Paquetes", "Lenguaje", "Otro"];
let razones = ["Facultad", "Proyecto Personal", "Trabajo"];

let categoria = await tp.system.suggester(categorias, categorias);
let razon = await tp.system.suggester(razones, razones);


// 2- DEFINICIÓN DE TAGS Y RENAME
let tagSoftware = "#" + titulo.replace(/\s+/g, '');
await tp.file.rename(titulo);


// 4-BYPASS DE FOCO
setTimeout(() => {
    const leaf = app.workspace.activeLeaf;
    if (leaf && leaf.view && leaf.view.editor) {
        const editor = leaf.view.editor;
        const lineas = editor.getValue().split('\n');
        
        // Foco específico en la descripción general del software
        const index = lineas.findIndex(l => l.includes("Descripción General"));
        
        if (index !== -1) {
            editor.setCursor({ line: index + 1, ch: 0 });
            editor.focus();
        }
    }
}, 400);
-%>
<div style="display: flex; flex-wrap: wrap; gap: 15px; padding: 12px 18px; margin-bottom: 20px; background-color: rgba(255, 255, 255, 0.05); border-radius: 8px; border-left: 4px solid #D095E8; font-size: 0.9em; align-items: center;">
  <span>📅 <b>Registro:</b> <% tp.date.now("DD-MM-YYYY") %></span>
  <span style="color: rgba(255, 255, 255, 0.2);">|</span>
  <span>🏷️ <b>Tags:</b> <% tagSoftware %> #Software #<% categoria %></span>
  <span style="color: rgba(255, 255, 255, 0.2);">|</span>
  <span>🎯 <b>Motivo:</b> <% razon %></span>
</div>

## <span style="color: #D095E8;">📄 Descripción General</span>


---
## <span style="color: #D095E8;">🏗️ Funcionamiento y Arquitectura</span>


---
## <span style="color: #D095E8;">🎯 ¿Para qué clase de proyectos se utiliza?</span>


---
## <span style="color: #D095E8;">⚙️ Notas de Configuración</span>























































Tags: <% tagSoftware %> #Software #<% categoria %> #<% razon %>